apiVersion: apps/v1
kind: Deployment
metadata:
  name: openstack-cluster-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: openstack-cluster-operator
  template:
    metadata:
      labels:
        name: openstack-cluster-operator
    spec:
      serviceAccountName: openstack-cluster-operator
      containers:
        - name: openstack-cluster-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - openstack-cluster-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "openstack-cluster-operator"
